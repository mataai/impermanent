---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
    ingress.kubernetes.io/force-ssl-redirect: "true"
    kubernetes.io/tls-acme: "true"
spec:
  ingressClassName: contour
  tls:
  - hosts:
    - matai-impermanent.sandbox.cedille.club # replace me!
    secretName: secret
  rules:
  - host: matai-impermanent.sandbox.cedille.club
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: impermanent-service
            port:
              number: 80
