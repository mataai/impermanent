---
apiVersion: v1
kind: Service
metadata:
  name: impermanent-backend-service
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: impermanent-backend
  ports:
  - port: <Port> #replace me (Port your app runs on)!
    targetPort: 80
